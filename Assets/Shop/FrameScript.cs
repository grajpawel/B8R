using UnityEngine;
using System.Collections;

public class FrameScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
		transform.position = new Vector2 (0, -1.1f); 
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
